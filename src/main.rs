use ember::window::ember_window::EmberWindow;

fn main()
{
    /* Code to help manually test visual display elements that can't be tested using the tests in
    the tests folder is to go here. */
    
    // https://crates.io/crates/pixels
    
    // https://crates.io/crates/miniquad
    
    // https://crates.io/crates/three-d
    // Looks like it might be the best option
    
    

    let mut test_window = EmberWindow::new();
    test_window.run();
}